#Цель кода:

#Цель данного кода - провести базовый анализ датасета, включая визуальное ознакомление с данными, выявление основных характеристик, чистку данных, а также создание нового датасета на основе выборки и преобразований.


import pandas as pd

# Загрузка датасета (предположим, что файл CSV используется)
df = pd.read_csv('C:\\Users\\20065\\OneDrive\\Рабочий стол\\Новая папка\\spotify_songs.csv')

print("Весь датасет:")
print(df)

# Вывести размерность массива
print("Размерность массива:", df.shape)

# Вывести наименование всех колонок, их описание и тип данных
print("\nНаименование колонок, описание и тип данных:")
for col in df.columns:
    print(f"{col} (тип данных: {df[col].dtype}): Описание")

# Вывести все уникальные значения в каждой колонке
print("\nУникальные значения в каждой колонке:")
for col in df.columns:
    print(f"{col}: {df[col].unique()}")

# Сортировать по дате выхода альбома
df_sorted = df.sort_values(by='track_album_release_date')

# Удалить ненужные столбцы или строки
df_cleaned = df.drop(['track_popularity'], axis=1)  # Удаление столбца
df_cleaned = df_cleaned.drop(index=[1, 2, 3])  # Удаление строк по индексу

# Заменить определенные значения в датасете (например, пустые значения на 'data')
df_filled = df.fillna(value='data')

# Удалить дубликаты
df_no_duplicates = df.drop_duplicates()

# Провести анализ с помощью функций info и describe
print("\nАнализ с помощью функции info:")
print(df.info())

print("\nАнализ с помощью функции describe:")
print(df.describe())

# Провести выборку данных по строкам и столбцам с помощью loc
selected_data = df.loc[df['track_artist'] == 'Zara Larsson', ['track_name', 'track_album_release_date']]

# Сохранить новый датасет
selected_data.to_csv('C:\\Users\\20065\\OneDrive\\Рабочий стол\\Новая папка\\dataset.csv', index=False)



#Вывод:

#Загрузка датасета:

#Датасет успешно загружен из файла 'spotify_songs.csv'.
#Вывод датасета:

#Датасет выведен для первичного визуального ознакомления с данными.
#Размерность массива:

#Датасет содержит X строк и Y столбцов (X, Y - фактические значения).
#Наименование колонок, описание и тип данных:

#Для каждой колонки выведены наименование, тип данных и краткое описание. Это позволяет лучше понять структуру данных.
#Уникальные значения в каждой колонке:

#Выведены уникальные значения для каждой колонки, что полезно для понимания разнообразия данных.
#Сортировка данных по дате выхода альбома:

#Данные отсортированы по колонке 'track_album_release_date'.
#Удаление ненужных столбцов или строк:

#Удален столбец 'track_popularity' и строки с определенными индексами.
#Замена определенных значений в датасете:

#Пустые значения в датасете заменены на строку 'data'.
#Удаление дубликатов:

#Проведено удаление дубликатов для поддержания чистоты данных.
#Анализ с помощью функций info и describe:

#Использованы функции info и describe для получения информации о структуре и статистике данных. Выведены типы данных, количество ненулевых значений, средние значения и другая статистика.
#Выборка данных по строкам и столбцам с помощью loc:

#Произведена выборка данных, относящихся к исполнителю 'Zara Larsson', включая только указанные колонки.
#Сохранение нового датасета:

#Создан и сохранен новый датасет 'dataset.csv' на рабочий стол с результатами изменений.

#Общий вывод:

#Проведенный базовый анализ данных позволяет лучше понять структуру датасета и подготовить данные для более глубокого анализа.
#Различные шаги по чистке данных, удалению дубликатов и выборке нужных данных важны для обеспечения качественного и точного анализа.
#Создание нового датасета на основе выборки может быть полезным при подготовке данных для конкретных задач и исследований.